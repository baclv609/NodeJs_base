<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>List product</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="container mx-auto">


        <h1 class="mb-5 text-center my-4">
            đây là list
        </h1>
        <a href="/admin/create"
            class="mt-5 text-white bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 me-2 mb-2">
            Add new product</a>
        <!-- component -->
        <div class="flex  items-center justify-center">

            <div class="overflow-x-auto">

                <table class="min-w-full bg-white shadow-md rounded-xl">
                    <thead>
                        <tr class="bg-blue-gray-100 text-gray-700">
                            <th class="py-3 px-4 text-left">ID</th>
                            <th class="py-3 px-4 text-left">Name</th>
                            <th class="py-3 px-4 text-left">Price</th>
                            <th class="py-3 px-4 text-left">Description</th>
                            <th class="py-3 px-4 text-left">Img</th>
                            <th class="py-3 px-4 text-left">Action</th>
                        </tr>
                    </thead>
                    <tbody class="text-blue-gray-900">
                        <% for (let i=0; i < products.length; i++) { %>
                            <tr class="border-b border-blue-gray-200">
                                <td class="py-3 px-4">
                                    <%= products[i].id %>
                                </td>
                                <td class="py-3 px-4">
                                    <%= products[i].name %>
                                </td>
                                <td class="py-3 px-4">
                                    <%= products[i].price %>
                                </td>
                                <td class="py-3 px-4">
                                    <%= products[i].description %>
                                </td>
                                <td class="py-3 px-4">
                                    <img style="width: 100px;" src="/uploads/<%=products[i].image %>" alt="">
                                </td>
                                <td class="py-3 px-4">
                                    <a href="/admin/edit?id=<%= products[i].id %>"
                                        class="font-medium text-blue-600 hover:text-blue-800">Edit</a>
                                    <a href="/admin/delete?id=<%= products[i].id %>"
                                        class="font-medium text-blue-600 hover:text-blue-800"
                                        onclick="return confirm('Are you sure you want to delete this product?')">DELETE</a>
                                </td>
                            </tr>
                            <% } %>
                    </tbody>
                </table>
                <div class="w-full pt-5 px-4 mb-8 mx-auto ">
                    <div class="text-sm text-gray-700 py-1 text-center">
                        Bản quyền thuộc về Bắc Lê
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>

</html>